# Unittests and Integration Tests — ALX Backend Python

This project focuses on writing **unit tests** and **integration tests** in Python using the `unittest` framework. It explores testing concepts such as mocking external calls, using fixtures, and parameterizing tests to reduce redundancy.

---

## 📌 Learning Objectives

By the end of this project, you should be able to:

- Explain the difference between **unit tests** and **integration tests**
- Use **mocking**, **parametrization**, and **fixtures** in Python tests
- Use `unittest` and `unittest.mock` to test functions and classes
- Write clean, maintainable, and fully documented test files
- Run tests using the CLI:  
  ```bash
  python3 -m unittest path/to/test_file.py
